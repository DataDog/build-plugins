{
    "name": "project-webpack4",
    "scripts": {
        "build": "yarn clean && NODE_OPTIONS=--openssl-legacy-provider webpack",
        "clean": "rm -rf ./dist ./webpack-profile-debug"
    },
    "devDependencies": {
        "webpack": "4.44.2",
        "webpack-cli": "4.1.0"
    },
    "dependencies": {
        "@datadog/webpack-plugin": "workspace:0.0.12-dev",
        "webpack4_app": "workspace:*"
    }
}

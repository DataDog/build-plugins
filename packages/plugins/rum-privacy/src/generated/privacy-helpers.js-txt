globalThis.$DD_ALLOW = new Set();
globalThis.$DD_ALLOW_OBSERVERS = new Set();
export function $(newValues) {
    const initialSize = globalThis.$DD_ALLOW.size;
    newValues.forEach((value) => globalThis.$DD_ALLOW.add(value));
    if (globalThis.$DD_ALLOW.size !== initialSize) {
        globalThis.$DD_ALLOW_OBSERVERS.forEach((cb) => cb());
    }
    return newValues;
}
